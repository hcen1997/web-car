# control car by func 
# go back left right

from gpiozero import Motor

def command(str):
    try:
        os.run(str)
    except expression as identifier:
        logging.error('')
    os.run(str)


